Code retired in the 6 August 2026 session, kept because it was deleted BEFORE
this repository existed and would otherwise survive nowhere.

  FinishAutoUpdateCommand.cs   Second ribbon button that drove the same finish
                               engine as Finish Surface Area. Its status readout
                               and on/off switch were folded into that command's
                               dialog; the button was removed as redundant.

  GenerateModelWindow.xaml     The single-page Vision Modeler dialog, replaced by
                               the two-step wizard. Its manual calibration fields
                               (pixel length, real length, datum X/Y) are gone for
                               good: the scale is now read off the placed image.

None of this compiles against the current source. It is here to answer "what did
it used to do?", not to be restored.
