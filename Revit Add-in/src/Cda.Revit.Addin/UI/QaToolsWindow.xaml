<Window x:Class="Cda.Revit.Addin.UI.QaToolsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QA Tools"
        Width="620"
        SizeToContent="Height"
        MinHeight="360"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Model quality checks"
                   FontSize="15"
                   FontWeight="SemiBold"
                   Margin="0,0,0,4" />

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,14">
            Every check here is READ-ONLY unless it says otherwise — nothing is written to the
            model. This window stays open while you work, so you can click a result and look at
            the element it found.
        </TextBlock>

        <!-- The checklist. One row per check; the button on each row is the whole
             interaction, so there is nothing to learn. -->
        <ItemsControl Grid.Row="2" x:Name="ChecklistPanel">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#FFD0D0D0"
                            BorderThickness="1"
                            Padding="12"
                            Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Description}"
                                           TextWrapping="Wrap"
                                           Opacity="0.8"
                                           Margin="0,4,0,0" />
                                <TextBlock Text="{Binding Status}"
                                           TextWrapping="Wrap"
                                           Opacity="0.7"
                                           FontStyle="Italic"
                                           Margin="0,6,0,0" />
                            </StackPanel>

                            <Button Grid.Column="1"
                                    Content="{Binding ActionLabel}"
                                    Tag="{Binding Key}"
                                    Width="120"
                                    Height="28"
                                    VerticalAlignment="Top"
                                    Click="OnRunCheck" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Grid.Row="3"
                   x:Name="DocumentLine"
                   TextWrapping="Wrap"
                   Opacity="0.65"
                   Margin="0,4,0,0" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Open log folder" Width="130" Height="28" Click="OnOpenLog" />
            <Button Content="Close" Width="90" Height="28" Margin="8,0,0,0" Click="OnClose" />
        </StackPanel>

    </Grid>
</Window>
